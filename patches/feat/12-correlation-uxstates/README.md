# FEAT-13 — Correlation Panel UX States (Sprint 9 — batch with FEAT-14)

Strips SPARK_DATA mock (hardcoded sparkline numbers).
Keeps DEF_RULES + DEF_INCIDENTS + SOURCES (DEFAULT fallback config pattern).

Wires VSPUXState skeleton into loadFromAPI() + initial render.
