1214

# Inline event handler baseline for VSP-CSP-001 Phase 2 ratchet-down.
#
# This number is the total count of inline event-handler attributes
# (onclick=, onchange=, onsubmit=, onload=, etc.) across all files in
# static/*.html at the moment Phase 1 completed.
#
# The CI guard (.github/workflows/csp-guard.yml handler-baseline job)
# reads the first integer token on the first line. Everything below is
# commentary and is ignored.
#
# Rules:
#   - Never increase this number. CI will fail on PRs that do.
#   - You may decrease this number only in the same PR that refactors
#     inline handlers to external listeners.
#
# Reference count methodology:
#   grep -rohE ' on[a-z]+=' static/ --include="*.html" | wc -l
#
# Baseline history:
#   753  — 2026-04-23  cf7fce4  Phase 1 snapshot at VSP-CSP-001 cutover
#   1214 — 2026-05-11           Re-baselined after phase1–phase4 panel
#                               buildup on docs/security-deliverables.
#                               The 461 delta is from organic panel
#                               additions (cicd, cspm, sso_admin, conmon,
#                               p4_compliance, sw_inventory, etc.) merged
#                               in batches over ~3 weeks. csp.go's
#                               docstring already anticipated ~1440
#                               panel inline handlers in steady state.
#                               Phase 2 refactor is still scheduled to
#                               bring this back down — see
#                               docs/CSP_HARDENING_ROADMAP.md.
#
# See docs/CSP_HARDENING_ROADMAP.md for the full plan.
